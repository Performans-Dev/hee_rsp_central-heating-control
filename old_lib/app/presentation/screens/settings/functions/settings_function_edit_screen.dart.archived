import 'package:flutter/material.dart';

class SettingsFunctionEditScreen extends StatelessWidget {
  const SettingsFunctionEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Settings / Functions / Edit'),
      ),
    );
  }
}
