import 'package:flutter/material.dart';


/// displays a user list on lock screen
/// when a user selected opens pincode screen
class LockUserListScreen extends StatelessWidget {
  const LockUserListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}