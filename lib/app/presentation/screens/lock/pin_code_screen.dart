import 'package:flutter/material.dart';

/// displays a keypad and waits for user to enter pin code
/// has an option to `forgot pin code`
class LockPinCodeScreen extends StatelessWidget {
  const LockPinCodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}