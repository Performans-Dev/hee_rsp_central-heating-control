import 'package:flutter/material.dart';

/// allows user to create a new pin code for himself/herself
class AppUserCreatePinCodeScreen extends StatelessWidget {
  const AppUserCreatePinCodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
