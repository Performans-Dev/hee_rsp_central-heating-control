import 'package:flutter/material.dart';

class HardwareInfoScreen extends StatelessWidget {
  const HardwareInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
