import 'package:flutter/material.dart';

class SettingsPreferencesAdvancedUpdatesScreen extends StatelessWidget {
  const SettingsPreferencesAdvancedUpdatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Settings / Preferences / Advanced / Updates')),
    );
  }
}
