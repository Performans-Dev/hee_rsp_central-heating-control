import 'package:flutter/material.dart';

class SettingsPreferencesDateFormatScreen extends StatelessWidget {
  const SettingsPreferencesDateFormatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Settings / Preferences / Date Format'),
      ),
    );
  }
}
