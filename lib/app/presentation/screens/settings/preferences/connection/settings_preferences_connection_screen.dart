import 'package:flutter/material.dart';

class SettingsPreferencesConnectionScreen extends StatelessWidget {
  const SettingsPreferencesConnectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Settings / Preferences / Connection'),
      ),
    );
  }
}
