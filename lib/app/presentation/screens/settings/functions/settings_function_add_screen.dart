import 'package:flutter/material.dart';

class SettingsFunctionAddScreen extends StatelessWidget {
  const SettingsFunctionAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Settings / Functions / Add'),
      ),
    );
  }
}
