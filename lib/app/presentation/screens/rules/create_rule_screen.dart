import 'package:flutter/material.dart';

class CreateRuleScreen extends StatelessWidget {
  const CreateRuleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
