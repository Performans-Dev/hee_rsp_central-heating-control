import 'package:flutter/material.dart';

class SigninForPinResetScreen extends StatelessWidget {
  const SigninForPinResetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SigninForPinResetScreen'),
      ),
    );
  }
}
