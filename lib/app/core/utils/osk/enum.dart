enum OnScreenKeyboardInputType {
  text,
  name,
  email,
  alphanumeric,
  number,
}

enum KeyType {
  character,
  backspace,
  enter,
  hideKeyboard,
  shift,
  alt,
}

enum OskType {
  all,
  upperCase,
  lowerCase,
  numbers,
  specialCharacters,
}
