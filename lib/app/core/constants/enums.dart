enum HardwareButtonLocation {
  l0,
  l1,
  l2,
  l3,
  r0,
  r1,
  r2,
  r3,
}

enum ConnectionState {
  none,
  connected,
}

enum JobState {
  idle,
  requested,
  success,
  error,
}

enum SnackbarType {
  info,
  success,
  warning,
  error,
}
