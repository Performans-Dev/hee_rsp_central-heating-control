enum NetworkIndicator {
  none,
  ethernet,
  wifi,
}

enum ScreenSaverType {
  staticPicture,
  slidePictures,
  fun,
}

enum DeviceType {
  heater,
  lamp,
  outletPlug,
  fan,
  cooler,
  gate,
  door,
  window,
}
